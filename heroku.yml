setup:
  addons:
    - plan: heroku-postgresql

build:
  docker:
    web: Dockerfile.prod
# RUN MIGRATIONS
#run:
 # web: node ./node_modules/typeorm/cli.js --config dist/database/ormconfig.js migration:run
